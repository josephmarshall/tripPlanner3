<div class="container">
<%# binding.pry %>
  <% if @location.name %>
    <%= @location.name %>
    <%= @location.address.street %>
    <%= @location.address.city %>
    <%= @location.address.state %>
    <%= @location.address.zip %>
  <% else%>
    <%= link_to 'New Address', new_trip_location_path(@trip) %>
  <% end %>
  
</div>